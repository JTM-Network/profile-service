server:
  port: 8222
  error:
    include-message: Always

spring:
  application:
    name: profile
  main:
    allow-bean-definition-overriding: true
  jackson:
    serialization:
      FAIL_ON_EMPTY_BEANS: false
  profiles:
    active: ${PROFILE}